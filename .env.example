# =============================================================================
# INVESTOR CALL NOTES CRM ASSISTANT - ENVIRONMENT VARIABLES
# =============================================================================
#
# Copy this file to .env and fill in your actual API keys and database IDs.
# DO NOT commit the .env file to version control!
#
# =============================================================================

# -----------------------------------------------------------------------------
# ANTHROPIC API KEY (Claude AI)
# -----------------------------------------------------------------------------
# Used for: Parsing meeting notes and extracting structured data
#
# Get your API key:
# 1. Visit https://console.anthropic.com/
# 2. Sign up or log in
# 3. Navigate to API Keys section
# 4. Click "Create Key" and copy it
#
# Format: sk-ant-api03-xxxxx...
# Cost: ~$0.01-0.05 per note (depending on length)
#
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# HUBSPOT API KEY
# -----------------------------------------------------------------------------
# Used for: Searching contacts, creating contacts, logging notes
#
# Get your API key:
# 1. Log in to your HubSpot account
# 2. Go to Settings → Integrations → Private Apps
# 3. Click "Create a private app"
# 4. Name it "Investor Notes CRM"
# 5. Under Scopes, select:
#    - crm.objects.contacts.read
#    - crm.objects.contacts.write
#    - crm.objects.notes.write
# 6. Click "Create app" and copy the access token
#
# Format: pat-na1-xxxxx... (private app token)
# Note: This is NOT your portal ID or user API key
#
HUBSPOT_API_KEY=your_hubspot_api_key_here

# -----------------------------------------------------------------------------
# HUBSPOT PORTAL ID
# -----------------------------------------------------------------------------
# Used for: Constructing links to HubSpot contacts
#
# Get your Portal ID:
# 1. Log in to your HubSpot account
# 2. The Portal ID is in the URL when viewing any page
#    Example: https://app.hubspot.com/contacts/12345678/contacts/list/view/all/
#    The Portal ID is: 12345678
# 3. Or go to Settings → Account Defaults → the number under "Hub ID"
#
# Format: Numeric ID (e.g., 12345678)
#
HUBSPOT_PORTAL_ID=your_portal_id_here

# -----------------------------------------------------------------------------
# NOTION API KEY
# -----------------------------------------------------------------------------
# Used for: Creating/updating investor preferences and todo items
#
# Get your API key:
# 1. Go to https://www.notion.so/my-integrations
# 2. Click "+ New integration"
# 3. Name it "Investor CRM Connector"
# 4. Select your workspace
# 5. Set capabilities: Read content, Update content, Insert content
# 6. Click "Submit" and copy the "Internal Integration Secret"
#
# IMPORTANT: After getting the key, you must SHARE your databases with the integration:
# 1. Open each database in Notion
# 2. Click ••• menu → Connections → Connect to
# 3. Select "Investor CRM Connector"
# 4. Click Confirm
#
# Format: secret_xxxxx...
# Common error: "object not found" means database isn't shared with integration
#
NOTION_API_KEY=your_notion_api_key_here

# -----------------------------------------------------------------------------
# NOTION INVESTOR PREFERENCES DATA SOURCE ID
# -----------------------------------------------------------------------------
# Used for: Storing and updating investor preferences
#
# IMPORTANT: This uses the Notion Data Sources API (v2025-09-03)
#
# Get your data source ID:
# 1. Open your Investor Preferences database in Notion
# 2. Click ••• menu → "Copy link"
# 3. URL format: https://www.notion.so/workspace/DATA_SOURCE_ID?v=view_id
# 4. Extract the DATA_SOURCE_ID (32 characters with dashes)
#
# Example: 234aba4f-8803-806d-8545-000bc560ccc9
#
# Note: The ID is the same format as a database ID, but we use it with the
# Data Sources API endpoint (/v1/data_sources/{id}/query) instead of the
# legacy Databases API endpoint.
#
# Required properties (case-sensitive):
# - Investor Name (title) - Company/fund name
# - Check Size (multi-select) - Investment amount range
# - Deal Structure (multi-select) - Preferred deal types
# - Style (multi-select) - Investment style
# - Industry (multi-select) - Preferred industries
# - Company Stage (multi-select) - Preferred company stages
# - Key Investment Criteria (multi-select) - Investment criteria
# - Capital Type (multi-select) - Type of capital
# - When to Call (multi-select) - When to reach out
# - Preference Notes (rich text) - Free-form notes
# - Primary Contact (rich text) - Name of the HubSpot contact (auto-populated)
# - Hubspot Link (url) - Link to the HubSpot contact record (auto-populated)
#
NOTION_INVESTOR_PREFS_DB_ID=234aba4f-8803-806d-8545-000bc560ccc9

# -----------------------------------------------------------------------------
# NOTION TODOS DATA SOURCE ID
# -----------------------------------------------------------------------------
# Used for: Creating follow-up tasks and action items
#
# IMPORTANT: This uses the Notion Data Sources API (v2025-09-03)
#
# Get your data source ID:
# 1. Open your TODOs database in Notion
# 2. Click ••• menu → "Copy link"
# 3. URL format: https://www.notion.so/workspace/DATA_SOURCE_ID?v=view_id
# 4. Extract the DATA_SOURCE_ID (32 characters with dashes)
#
# Example: 49025339-77f1-4857-993c-337bb7d3eec3
#
# Note: The ID is the same format as a database ID, but we create pages using
# parent type "data_source_id" instead of "database_id".
#
# Required properties (case-sensitive):
# - Task Name (title) - Name of the task
# - Manual Due (date) - Due date for the task
# - Next Step (rich text) - Description of next action
#
NOTION_TODOS_DB_ID=49025339-77f1-4857-993c-337bb7d3eec3

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# SERPER API KEY (Google Search)
# -----------------------------------------------------------------------------
# Used for: Web search to find LinkedIn profiles and recent activity
#
# Get your API key:
# 1. Visit https://serper.dev/
# 2. Sign up for a free account (2,500 free searches)
# 3. Or subscribe: $5/month for 5,000 searches
# 4. Go to Dashboard → API Key
# 5. Copy your API key
#
# Format: xxxxx... (alphanumeric string)
# Cost: Free tier available, then $5 for 5,000 searches
#
# NOTE: If not provided, web search will be disabled
# The call brief will still work, but won't include LinkedIn/web activity
#
SERPER_API_KEY=your_serper_api_key_here

# -----------------------------------------------------------------------------
# DAILY DEAL REMINDER EMAIL CONFIGURATION
# -----------------------------------------------------------------------------
# Used for: Sending automated email reminders for deals with next steps due TOMORROW
# Note: Reminders are sent for deals due the NEXT day, giving you time to prepare
#
# REMINDER_ENABLED: Set to 'true' to enable daily email reminders, 'false' to disable
# Format: true or false
#
REMINDER_ENABLED=false

# REMINDER_TIME: Time to send daily reminders (24-hour format, UTC timezone)
# Format: HH:MM (e.g., 08:00 for 8 AM UTC, 14:30 for 2:30 PM UTC)
# Default: 08:00 (8 AM UTC)
#
# Note: Make sure to convert your local time to UTC
# Example: If you want reminders at 8 AM EST (UTC-5), set this to 13:00
#
REMINDER_TIME=08:00

# REMINDER_EMAIL_TO: Your email address (where reminders will be sent)
# Format: email@example.com
#
REMINDER_EMAIL_TO=your-email@example.com

# REMINDER_EMAIL_FROM: Sender email address (must be authorized on your SMTP server)
# Format: email@example.com
# Note: For most SMTP providers, this should match your SMTP_USERNAME
#
REMINDER_EMAIL_FROM=your-email@example.com

# -----------------------------------------------------------------------------
# SMTP SERVER CONFIGURATION (for sending emails)
# -----------------------------------------------------------------------------
# You can use various SMTP providers:
#
# Option 1: Gmail
# - SMTP_SERVER=smtp.gmail.com
# - SMTP_PORT=587
# - SMTP_USERNAME=your-email@gmail.com
# - SMTP_PASSWORD=your-app-specific-password (NOT your regular Gmail password!)
# - Get app password: https://myaccount.google.com/apppasswords
#
# Option 2: Outlook/Office 365
# - SMTP_SERVER=smtp.office365.com
# - SMTP_PORT=587
# - SMTP_USERNAME=your-email@outlook.com
# - SMTP_PASSWORD=your-password
#
# Option 3: SendGrid
# - SMTP_SERVER=smtp.sendgrid.net
# - SMTP_PORT=587
# - SMTP_USERNAME=apikey
# - SMTP_PASSWORD=your-sendgrid-api-key
#
# Option 4: Render (if you have a custom domain)
# - Check your Render email service configuration
# - Or use any of the above providers
#
# Option 5: Other SMTP providers (AWS SES, Mailgun, etc.)
# - Check your provider's SMTP configuration
#

# SMTP_SERVER: SMTP server hostname
# Format: smtp.example.com
#
SMTP_SERVER=smtp.gmail.com

# SMTP_PORT: SMTP server port (usually 587 for TLS, 465 for SSL)
# Format: Numeric port (587 is most common)
#
SMTP_PORT=587

# SMTP_USERNAME: SMTP authentication username
# Format: Usually your email address
#
SMTP_USERNAME=your-email@gmail.com

# SMTP_PASSWORD: SMTP authentication password
# Format: Your email password or app-specific password
# IMPORTANT: For Gmail, you MUST use an app-specific password, not your regular password
# Get one here: https://myaccount.google.com/apppasswords
#
SMTP_PASSWORD=your-app-password-here

# Flask debug mode (set to False in production)
# FLASK_DEBUG=True

# Flask port (default: 5000)
# FLASK_PORT=5000

# =============================================================================
# NOTES
# =============================================================================
#
# Security:
# - Never commit .env to version control
# - .gitignore includes .env by default
# - Rotate keys every 90 days
# - Use secrets management in production (not .env files)
#
# Troubleshooting:
# - If variables don't load: ensure no quotes around values
# - If Notion errors: verify databases are shared with integration
# - If HubSpot errors: verify private app has correct scopes
# - Check app.py console for missing variable warnings
#
# For detailed setup instructions, see README.md
#
# =============================================================================
